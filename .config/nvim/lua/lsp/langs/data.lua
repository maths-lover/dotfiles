-- lua/lsp/langs/data.lua - config/data formats: JSON, YAML, TOML.
-- taplo is both the TOML LSP and its formatter (see conform).
return {
  servers = {
    jsonls = {},
    yamlls = {},
    taplo = {},
  },
}
